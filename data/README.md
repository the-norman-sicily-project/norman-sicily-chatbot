# Data Directory

This directory is gitignored and should contain your local data files for the Norman Sicily Historical Chatbot.

Required files (by default):
- people_to_places.csv
- places_to_places.csv
- combined_output.txt

You can change the data directory by setting the DATA_DIR environment variable.
